//WORDT NIET GEBRUIKT WEGGOOIEN DURF IK NIET

export class CharacterStore {
  constructor(store) {
    this.store = store;
    this.all = RequestBody<T>;
    this.single = {};
    this.loading = false;
    makeAutoObservable(this);
  }
}
